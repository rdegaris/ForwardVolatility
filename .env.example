# Copy to .env (or .secrets.env) and fill in values. Do NOT commit .env.

# Finnhub API key (used for earnings calendar)
FINNHUB_API_KEY=

# Optional: IB settings overrides
# IB_HOST=127.0.0.1
# IB_PORT=7498
# IB_CLIENT_ID=110
